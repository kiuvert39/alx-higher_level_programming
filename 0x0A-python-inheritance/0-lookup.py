def lookup(obj):

    """ this function returns the methods, attribute in  an  object
        Args:
            obj:  this the actual object  that contain the attribute and the methods 
    """ 

    return  (dir(obj))
